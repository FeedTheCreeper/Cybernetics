package main.cybernetics.blocks;

import net.minecraft.block.material.Material;
import net.minecraft.block.Block;
import net.minecraft.creativetab.CreativeTabs;

public class BlockChromite extends Block 
{
        public BlockChromite(Material material) 
        {
        	super(material);
        	setHardness(4.0F); // 33% harder than diamond
        	setStepSound(Block.soundTypePiston); // sounds got renamed, look in Block class for what blocks have what sounds
        	setBlockName("chromiteOre"); // changed in 1.7
        	setCreativeTab(CreativeTabs.tabBlock);
        }
}
